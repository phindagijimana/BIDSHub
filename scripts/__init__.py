"""BIDSHub maintenance and migration scripts.

Marked as a package so it imports reliably from a frozen bundle (and anywhere
sys.path includes the repo root), not only when the CWD is the repo root.
"""
